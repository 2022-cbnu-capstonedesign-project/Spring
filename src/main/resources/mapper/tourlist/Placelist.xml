<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cbnu.capstone.tripwithme.mapper.placelist.PlacelistMapper">

    <select id="getTourlist" resultType="cbnu.capstone.tripwithme.dto.placelist.PlacelistDTO">
        SELECT * FROM placelist;
    </select>

    <select id="'getTourlistFilter" resultType="cbnu.capstone.tripwithme.dto.placelist.PlacelistDTO">
        SELECT * FROM placeList where = #{region}
    </select>

</mapper>